const openProfileOptions = () => {
    const 
}

window.addEventListener('load', () => {
    const profileButton = document.querySelector(".profile-icon")
    
    profileButton.addEventListener('click', openProfileOptions())
})